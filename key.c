#include<stdio.h>
int search(int arr[],int key,int n)
{
    for(int i=0; i<n; i++)
    {
        if(arr[i]==key)
    }
    return 0;
}
void main()
{
    int key;
    printf("enter key");
    scanf("%d",key);
    search(key);
}